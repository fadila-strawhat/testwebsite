Build a program that:
•	Creates an array of student names. 
•	Creates another array of their scores. 
•	Uses a loop to display each student's name and score. 
•	Calculates the total number of students. 
•	Checks whether a particular student exists using includes(). 
•	Allows you to add a new student using push(
